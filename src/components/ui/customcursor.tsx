'use client'

import { useCustomCursor } from '@/hooks/usecustomcursor'

export default function CustomCursor() {
  // const { cursorRef } = useCustomCursor()
  
  return (
    <div
      // ref={cursorRef}
      // className="fixed w-5 h-5 border border-terminal-border rounded-full pointer-events-none z-50 transition-all duration-100 mix-blend-difference"
    />
  )
}